[build]
builder = "nixpacks"
buildCommand = "npm install --prefer-offline --no-audit --no-fund && npm run build"

[deploy]
startCommand = "npx serve -s dist -l tcp://0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
